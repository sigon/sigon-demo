<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd"
       default-lazy-init="true">

    <bean id="freeMarkerConfigurer" class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
        <property name="templateLoaderPaths" value="${template.loader_path}" />
        <property name="freemarkerSettings">
            <props>
                <prop key="defaultEncoding">${template.encoding}</prop>
                <prop key="tag_syntax">auto_detect</prop>
                <prop key="whitespace_stripping">true</prop>
                <prop key="classic_compatible">true</prop>
                <prop key="template_update_delay">${template.update_delay}</prop>
                <prop key="object_wrapper">freemarker.ext.beans.BeansWrapper</prop>
                <prop key="datetime_format">${template.datetime_format}</prop>
                <prop key="date_format">${template.date_format}</prop>
                <prop key="time_format">${template.time_format}</prop>
                <prop key="number_format">${template.number_format}</prop>
                <prop key="boolean_format">${template.boolean_format}</prop>
                <prop key="auto_import">include/aunewtop.ftl as aunewtop</prop>
            </props>
        </property>
        <property name="freemarkerVariables">
            <map>
                <entry key="systemName" value="${system.project_name}" />
                <entry key="systemVersion" value="${system.version}" />
                <entry key="systemDescription" value="${system.description}" />
                <entry key="locale" value="${locale}" />
                <entry key="base" value="${serviceside.domain.url}" />
                <!--<entry key="pagination" value-ref="paginationDirective" />-->
            </map>
        </property>
    </bean>
    <bean id="profileInvokeLogAdvice" class="com.sigon.demo.web.interceptor.ProfilerInterceptor">
        <property name="switchOn">
            <value>true</value>
        </property>
        <property name="threshold">
            <value>200</value>
        </property>
    </bean>
    <!--<bean id="profileInvokeLogInterceptor" class="org.springframework.aop.support.DefaultPointcutAdvisor">-->
        <!--<property name="advice" ref="profileInvokeLogAdvice"/>-->
    <!--</bean>-->
    <bean class="org.springframework.aop.framework.autoproxy.BeanNameAutoProxyCreator">
        <property name="interceptorNames">
            <list>
                <idref bean="profileInvokeLogAdvice"/>
            </list>
        </property>
        <property name="beanNames">
            <list>
                <!--由于和自定义的manager,以及Service冲突，不能支持*的写法，只能单独写要监控的beanid-->
                <value>*Mapper</value>
            </list>
        </property>
    </bean>

</beans>